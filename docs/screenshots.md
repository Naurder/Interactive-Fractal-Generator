# Screenshot selection

Recommended `README.md` preview:

```text
screenshots/preview.png
```

Suggested source image from the original archive:

```text
Zrzut ekranu 2023-01-13 120808.png
```

Why this one:

- it shows the actual GUI,
- the fractal is visually strong,
- it communicates that the project is interactive, not only a static generator.

Suggested cleanup:

```text
screenshots/
├── preview.png
├── fractal-tree-purple.png
├── fractal-tree-rainbow.png
├── sierpinski-triangle.png
├── koch-snowflake.png
├── corridor.png
└── spiral-fractal.png
```

Avoid committing many duplicate screenshots. A portfolio repo should look curated, not like a raw export folder.
